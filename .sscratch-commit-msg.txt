fix: address PR #30 review findings (round 5, 4 items)

Services (skill/subagent):
- uninstall: only remove projections for agents the asset is actually
  enabled on — a disabled agent's same-named path may be independently
  installed user content and must not be deleted
- reuse_existing_install: sync the projection before persisting the
  updated agent flags (sync-first, matching toggle_app), so a failed
  projection no longer leaves the DB claiming an agent is enabled with
  no usable projection

Settings:
- set_agent_override_dir: on the success path, persist the overwritten
  live content at the new override as a disabled backup preset
  (backup-<timestamp>, deduped against existing presets) instead of
  discarding new_live_backup; backup failures participate in the
  existing rollback

Frontend:
- discovery pages (skills + subagents): uninstall now requires a
  confirmation dialog (existing skill-dialog pattern) before invoking
  the uninstall mutation, per the frontend contract doc

Tests: 6 new Rust regressions + 4 new/updated L1 frontend cases;
124 cargo tests (both feature sets), clippy/fmt clean,
verify:static 8/8, vitest 143, L2 4 specs, L3 1/1.
